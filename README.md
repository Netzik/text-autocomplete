# Автодополнение текстов (Next Token Prediction) 


## 📱 Бизнес-контекст

Данный проект выполнен в рамках задачи для социальной сети (аналог Twitter/X), где пользователи публикуют короткие текстовые сообщения (посты). 
**Цель:** Разработать модель машинного обучения для **автодополнения текста** (next token prediction).

**Ключевые требования:**
- 🚀 **Низкая задержка:** модель должна работать быстро на смартфоне.
- 📦 **Малый вес:** ограничения по оперативной памяти мобильных устройств.
- 🎯 **Качество:** предсказания должны быть релевантными контексту.

## ⚙️ Стек 

- **Язык:** Python 
- **Фреймворк:** PyTorch 
- **Архитектуры:**
    - Кастомная **LSTM** 
    - **DistilGPT2** (для сравнения)
- **Метрики качества:** ROUGE-1, ROUGE-2, ROUGE-L



<small>


```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# venv\Scripts\activate   # для Windows

# Установить зависимости
pip install -r requirements.txt

# Для GPU версии PyTorch (если нужно):
# pip install torch==2.0.1+cu118 --extra-index-url https://download.pytorch.org/whl/cu118
